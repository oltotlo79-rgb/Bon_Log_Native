# ナビゲーション・画面構成仕様 — Bon_Log Native

作成日: 2026-06-12
更新日: 2026-06-20（ボトムナビを 5 タブに変更・「もっと見る」タブを追加。§4.2 もっと見る画面を Web 版構成に合わせて更新）
追記（2026-07-13）: §1 の `app/` ツリー図を実装（Glob 実測）に合わせて全面更新した（イベント・盆栽園・辞典・農薬・施肥・ホルモン・投稿分析・マイ盆栽・手入れログ・予約投稿・ブックマーク・発見・法的文書・メッセージ・フォローリクエスト・2段階認証等、2026-06-20 以降に実装された全ルートを反映）。あわせて §2.1（メッセージの扱い）・§4.1（2FA 画面の追加）・§4.2（もっと見る画面概要。`more-menu.md` の同日改訂に追従）・§9（解消済み事項の整理）を更新した。
対象: `app/` ディレクトリ（Expo Router）の構成と各画面の概要
前提: MVP スコープ（フィード / 投稿 CRUD / コメント / いいね / フォロー / 通知 / プロフィール / 検索）

---

## 1. 全体構造

```
app/
├── _layout.tsx                    # ルートレイアウト（Provider群 + 認証ガード）
├── index.tsx                      # ルート（(tabs)/feed へ redirect）
│
├── (auth)/                        # 未認証スタック（ログイン前）
│   ├── _layout.tsx                # 認証済みの場合フィードへリダイレクト
│   ├── login/
│   │   └── index.tsx              # ログイン
│   ├── register/
│   │   ├── index.tsx              # 新規登録
│   │   └── verify-email-sent/
│   │       └── index.tsx          # メール確認送信完了
│   ├── password-reset/
│   │   ├── index.tsx              # パスワードリセット申請
│   │   └── confirm/
│   │       └── index.tsx          # パスワードリセット確認
│   └── two-factor-verify/
│       └── index.tsx              # 2段階認証（2FA）コード入力
│
├── (tabs)/                        # 認証後ボトムタブ
│   ├── _layout.tsx                # BottomTabBar 定義（5 タブ）
│   ├── feed/
│   │   └── index.tsx              # フィード（タイムライン）
│   ├── search/
│   │   └── index.tsx              # 検索
│   ├── notifications/
│   │   └── index.tsx              # 通知
│   ├── profile/
│   │   └── index.tsx              # 自分のプロフィール
│   └── more/
│       └── index.tsx              # もっと見る（メニュー画面。詳細: more-menu.md）
│
├── posts/
│   ├── [id]/
│   │   ├── index.tsx              # 投稿詳細（コメント含む）
│   │   ├── edit/
│   │   │   └── index.tsx          # 投稿編集
│   │   └── quote/
│   │       └── index.tsx          # 引用投稿作成
│   └── new/
│       └── index.tsx              # 新規投稿
│
├── users/
│   └── [id]/
│       ├── index.tsx              # 他ユーザープロフィール
│       ├── followers/
│       │   └── index.tsx          # フォロワー一覧
│       ├── following/
│       │   └── index.tsx          # フォロー中一覧
│       └── likes/
│           └── index.tsx          # いいねした投稿一覧
│
├── messages/                      # DM（ダイレクトメッセージ）
│   ├── index.tsx                  # 会話一覧
│   └── [conversationId]/
│       └── index.tsx              # 会話スレッド
│
├── follow-requests/
│   └── index.tsx                  # フォローリクエスト管理
│
├── settings/
│   ├── index.tsx                  # 設定トップ
│   ├── profile/
│   │   └── index.tsx              # プロフィール編集
│   ├── account/
│   │   └── index.tsx              # アカウント設定（削除導線含む）
│   ├── notifications/
│   │   └── index.tsx              # 通知設定
│   ├── security/
│   │   └── index.tsx              # セキュリティ設定（2段階認証等）
│   ├── password/
│   │   └── index.tsx              # パスワード変更
│   ├── email/
│   │   └── index.tsx              # メールアドレス変更
│   ├── blocked/
│   │   └── index.tsx              # ブロックリスト
│   ├── muted/
│   │   └── index.tsx              # ミュートリスト
│   └── subscription/
│       └── index.tsx              # サブスクリプション（購入・復元）
│
├── explore/                       # 発見（ウェーブ1）
│   ├── index.tsx                  # 発見トップ
│   └── posts/
│       └── index.tsx              # ハッシュタグ / ジャンル別投稿一覧
│
├── dictionary/                    # 盆栽用語辞典（ウェーブ1）
│   ├── index.tsx                  # 辞典一覧
│   └── [slug]/
│       └── index.tsx              # 辞典詳細
│
├── fertilizers/                   # 施肥ガイド（ウェーブ1）
│   ├── index.tsx                  # 施肥ガイドトップ
│   ├── absorption/
│   │   └── index.tsx              # 栄養素の吸収と転流
│   ├── categories/
│   │   └── index.tsx              # 肥料カテゴリ比較
│   ├── columns/
│   │   ├── index.tsx              # 肥料コラム一覧
│   │   └── [slug]/
│   │       └── index.tsx          # 肥料コラム詳細
│   ├── nutrients/
│   │   ├── index.tsx              # 栄養素辞典一覧
│   │   └── [slug]/
│   │       └── index.tsx          # 栄養素詳細
│   ├── products/
│   │   └── index.tsx              # 定番肥料ガイド
│   ├── schedules/
│   │   └── index.tsx              # 樹種別施肥スケジュール一覧
│   ├── soil/
│   │   └── index.tsx              # 用土ガイド
│   ├── symptoms/
│   │   └── index.tsx              # 欠乏症状ガイド
│   ├── tree-species/
│   │   └── [slug]/
│   │       └── index.tsx          # 施肥樹種別詳細
│   ├── troubles/
│   │   └── index.tsx              # 施肥トラブル事例集
│   └── watering/
│       └── index.tsx              # 水やりガイド
│
├── hormones/                      # 植物ホルモン（ウェーブ1）
│   ├── index.tsx                  # ホルモントップ
│   ├── [slug]/
│   │   └── index.tsx              # ホルモン詳細
│   ├── calendar/
│   │   └── index.tsx              # 年間ホルモン活性カレンダー
│   ├── columns/
│   │   ├── index.tsx              # ホルモンコラム一覧
│   │   └── [slug]/
│   │       └── index.tsx          # ホルモンコラム詳細
│   ├── diagram/
│   │   └── index.tsx              # 相互作用ダイアグラム
│   ├── interactions/
│   │   └── index.tsx              # 相互作用一覧
│   ├── simulator/
│   │   └── index.tsx              # ホルモンバランスシミュレーター
│   └── techniques/
│       └── index.tsx              # 技法とホルモン
│
├── pesticides/                    # 農薬・病害虫図鑑（ウェーブ1）
│   ├── index.tsx                  # 農薬トップ
│   ├── columns/
│   │   ├── index.tsx              # 農薬コラム一覧
│   │   └── [slug]/
│   │       └── index.tsx          # 農薬コラム詳細
│   ├── dilution-calculator/
│   │   └── index.tsx              # 希釈計算ツール
│   ├── disease-pests/
│   │   ├── index.tsx              # 病害虫一覧
│   │   └── [slug]/
│   │       └── index.tsx          # 病害虫詳細
│   ├── formulations/
│   │   └── index.tsx              # 剤型一覧
│   ├── ingredients/
│   │   ├── index.tsx              # 有効成分一覧
│   │   └── [slug]/
│   │       └── index.tsx          # 有効成分詳細
│   ├── mixing-checker/
│   │   └── index.tsx              # 混用チェッカー
│   ├── products/
│   │   └── [slug]/
│   │       └── index.tsx          # 農薬製品詳細
│   ├── spray-guide/
│   │   └── index.tsx              # 散布方法ガイド
│   └── spreaders/
│       ├── index.tsx              # 展着剤一覧
│       └── [slug]/
│           └── index.tsx          # 展着剤詳細
│
├── legal/                         # 法的文書（ウェーブ1）
│   ├── index.tsx                  # 法的文章一覧
│   └── [slug]/
│       └── index.tsx              # 法的文章詳細（利用規約 / プライバシーポリシー / 特商法表記）
│
├── analytics/
│   └── index.tsx                  # 投稿分析（プレミアム限定）
│
├── bonsai/                        # マイ盆栽（ウェーブ2）
│   ├── index.tsx                  # マイ盆栽一覧
│   ├── new/
│   │   └── index.tsx              # 盆栽新規登録
│   ├── care-logs/
│   │   └── index.tsx              # 手入れログ一覧（カレンダー）
│   └── [id]/
│       ├── index.tsx              # 盆栽詳細
│       ├── edit/
│       │   └── index.tsx          # 盆栽編集
│       └── records/
│           ├── new/
│           │   └── index.tsx      # 成長記録の新規追加
│           └── [recordId]/edit/
│               └── index.tsx      # 成長記録の編集
│
├── events/                        # イベント（ウェーブ2）
│   ├── index.tsx                  # イベント一覧
│   ├── new/
│   │   └── index.tsx              # イベント新規登録
│   └── [id]/
│       ├── index.tsx              # イベント詳細
│       └── edit/
│           └── index.tsx          # イベント編集
│
├── shops/                         # 盆栽園マップ（ウェーブ2）
│   ├── index.tsx                  # 盆栽園マップ一覧
│   ├── new/
│   │   └── index.tsx              # 盆栽園新規登録
│   └── [id]/
│       ├── index.tsx              # 盆栽園詳細
│       ├── edit/
│       │   └── index.tsx          # 盆栽園編集
│       └── reviews/
│           ├── index.tsx          # 盆栽園レビュー一覧
│           └── new/
│               └── index.tsx      # 盆栽園レビュー新規投稿
│
├── bookmarks/
│   └── index.tsx                  # ブックマーク一覧
│
└── scheduled-posts/                # 予約投稿（プレミアム限定・ウェーブ2）
    ├── index.tsx                  # 予約投稿一覧
    ├── new/
    │   └── index.tsx              # 予約投稿新規作成
    ├── locked/
    │   └── index.tsx              # 非プレミアムユーザー向けロック画面
    └── [id]/
        ├── index.tsx              # 予約投稿詳細
        └── edit/
            └── index.tsx          # 予約投稿編集
```

> **注記（2026-07-13）:** 上記ツリーは `app/` 配下の Glob 実測に基づいて更新済み（全 103 画面ファイル + ルートレイアウト 3 件を反映）。個別画面の詳細仕様は各設計ドキュメント（`docs/design/events.md`・`docs/design/shops.md`・`docs/design/bonsai.md`・`pesticides-web-parity.md`・`hormones-fertilizers-web-parity.md`・`events-shopmap-web-parity.md` 等）を参照。過度に詳細化せず主要スクリーン群を反映する粒度で維持しており、実装変更のたびに厳密な同期を保証するものではない。

---

## 2. ボトムタブ構成

### 2.1 タブ一覧

| タブ番号 | ラベル | アイコン（Lucide 系） | ルート | Web 対応 |
|---------|------|--------------------|-------|--------|
| 1 | ホーム | `Home` | `(tabs)/feed` | `/feed` |
| 2 | 検索 | `Search` | `(tabs)/search` | `/search` |
| 3 | 通知 | `Bell` | `(tabs)/notifications` | `/notifications` |
| 4 | プロフィール | `User` | `(tabs)/profile` | `/users/[自分のID]` |
| 5 | もっと見る | `MoreHorizontal` | `(tabs)/more` | Web 版の「もっと見る」メニューに相当 |

**Web 版との差異:** Web の MobileNav には「ホーム / 検索 / 通知 / メッセージ」の 4 タブ + 「もっと見る」があるが、モバイルアプリでは「プロフィール」と「もっと見る」をタブとして設ける 5 タブ構成とする。メッセージ（DM）は当初 MVP スコープ外としていたが、後続ウェーブでネイティブ画面（`messages/`）として実装され、ボトムタブではなく「もっと見る」経由のスタック画面として提供する（**2026-07-13 更新:** 実装を反映）。

**「もっと見る」タブの役割:** アプリ内のすべての機能とコンテンツへのアクセス入口を 1 画面にまとめる。項目のほぼすべて（ヘルプを除く）はネイティブ遷移で提供し、ヘルプのみ expo-web-browser でアプリ内ブラウザを開く（**2026-07-13 更新:** 旧版は大半の項目をアプリ内ブラウザで開く前提だったが、実装はネイティブ遷移が主。詳細は `docs/design/more-menu.md` を参照）。

### 2.2 BottomTabBar 仕様

```
高さ: 60pt（セーフエリア下端は別途追加）
背景: colorNavBackground (#f7f7f7) / backdrop blur（不透明度 90%）
上端境界: 1pt solid colorBorderLight
アイコンサイズ: 20 x 20pt
タップターゲット: 幅は 1/5 画面幅（flex: 1）、高さは 60pt — 両軸とも 44pt 以上
ラベル: textXs（10pt） / letterSpacingWidest（1.5） / 上マージン 4pt
アクティブ色: colorNavIconActive（#252525）/ fontWeight: 700
非アクティブ色: colorNavIconInactive（#72727280）
アクティブインジケータ: アイコン上部の小円点 + アイコン下部の墨筆風短線（Web の ActiveBarIndicator と同じ意匠、React Native の SVG で再現）
通知バッジ: 通知タブのアイコン右上に赤いバッジ（未読数 or 点のみ）
```

**タップターゲット幅の変更:** 4 タブ時は `1/4 画面幅` だったが、5 タブ化により `1/5 画面幅` に変わる。375pt 幅の端末では 1 タブあたり 75pt となり、44pt 最小要件は引き続き満たす。

**iOS / Android 差異:**
- iOS は SafeAreaView で下端の home indicator エリアを自動確保する。
- Android では NavigationBar の高さを `react-native-safe-area-context` で取得してパディングを適用する。

---

## 3. ルートレイアウト（認証ガード）

`app/_layout.tsx` に認証状態の参照と画面振り分けを集約する（画面ごとの個別ガードは作らない）。

```
起動時の処理フロー:
1. expo-secure-store からトークンを確認（lib/auth/ 経由）
2. 未認証 → (auth)/login へ redirect
3. 認証済みが (auth) 配下 → (tabs)/feed へ redirect
4. ディープリンクで保護画面に到達 → ログイン後に元の画面へ復帰（lib/push/ の対応表を参照）
```

---

## 4. 各画面の概要と必須状態

### 4.1 (auth) スタック

#### ログイン画面 `(auth)/login`

**概要:** メールアドレス + パスワードによるログイン。Google OAuth オプション。

**画面要素:**
```
[ヘッダー: アプリロゴ + 「BON-LOG」]
[見出し: 「ログイン」]
[メールアドレス入力]
[パスワード入力]
[ログインボタン（主要アクション）]
[区切り]
[Google でログイン]
[新規登録へのリンク]
[パスワードリセットへのリンク]
```

**状態:**
- ローディング: ボタンをスピナーに変えて連打防止
- エラー: 「メールアドレスまたはパスワードが正しくありません」（インラインエラー）
- オフライン: 「接続を確認してください。ネットワークが利用できない状態です。」

---

#### 新規登録画面 `(auth)/register`

**画面要素:**
```
[見出し: 「新規登録」]
[ニックネーム入力]
[メールアドレス入力]
[パスワード入力]
[登録ボタン]
[区切り]
[Google で登録]
[ログインへのリンク]
```

---

#### パスワードリセット画面 `(auth)/password-reset`

**画面要素:**
```
[見出し: 「パスワードの再設定」]
[メールアドレス入力]
[送信ボタン]
```

送信完了後 → 「メールをご確認ください」画面へ遷移。

---

#### 2 段階認証コード入力画面 `(auth)/two-factor-verify`

**概要:** メール/パスワードログイン成功後、サーバーが 2FA 必須（`requires2FA: true` + `ticket`）を返した場合に表示する TOTP / バックアップコード入力画面。ticket は単回使用・TTL 300 秒のため、失敗時は自動リトライできずログイン画面からやり直す設計。詳細仕様は `docs/design/two-factor-auth.md` を参照。

**画面要素:**
```
[ヘッダー: 「2 段階認証」]
[シールドアイコン]
[説明文: 認証アプリの 6 桁コードを入力するよう促す]
[認証コード入力フィールド（TOTP / バックアップコード切替可）]
[フォーム全体エラー（表示時のみ）]
[確認するボタン]
[バックアップコードを使用する（切替リンク）]
[ログイン画面に戻る（リンク）]
```

**状態:**
- コード不正・チケット期限切れ・レート制限: フォーム全体を無効化し「ログイン画面へ戻る」ボタンを強調表示（ticket 再利用不可のため）
- ネットワークエラー・5xx: フォームは有効のまま維持（再試行可能）

---

### 4.2 (tabs) — ボトムタブ画面

#### フィード画面 `(tabs)/feed`

**概要:** フォロー中ユーザーの投稿が時系列で並ぶタイムライン。MVPのコア画面。

**画面要素:**
```
[ナビゲーションヘッダー]
  左: アプリロゴ（小）
  中央: 「ホーム」
  右: （将来: フィルター）

[フィードリスト（FlatList / 無限スクロール）]
  各 PostCard:
    [アバター（44pt）]
    [ユーザー名 + 日時]
    [投稿本文（最大 500/2000 文字）]
    [画像ギャラリー（1〜4枚）]
    [ジャンルタグ（最大 3 つ）]
    [アクションバー: いいね / コメント / リポスト / ブックマーク]

[FAB: 投稿ボタン（右下固定 / BottomTabBar 上 spacing4 + 60pt）]

[Pull-to-refresh: 引っ張って最新を取得]
```

**状態:**

| 状態 | 表示 |
|------|------|
| ローディング | PostCard スケルトン x 3 を縦並び |
| 空 | イラスト + 「まだ投稿がありません。フォローしてみましょう。」+ 検索へのリンク |
| エラー | 「タイムラインを読み込めませんでした。」+ 再読み込みボタン |
| オフライン | 画面上部にバナー「オフライン中です。接続が回復したら自動的に更新されます。」|

---

#### 検索画面 `(tabs)/search`

**概要:** ユーザー・ハッシュタグ・投稿の全文検索。

**画面要素:**
```
[検索バー（画面幅いっぱい / 上部固定）]
  プレースホルダー: 「ユーザー、ハッシュタグ、投稿を検索」

[入力前]
  [トレンドタグ（上位ハッシュタグ）]
  [おすすめユーザー（フォロー候補）]

[入力後・検索中]
  [タブ: ユーザー / 投稿 / ハッシュタグ]
  各タブのリスト結果
```

**状態:**

| 状態 | 表示 |
|------|------|
| ローディング（検索実行中） | リストスケルトン |
| 空（結果なし） | 「「{キーワード}」に一致する結果が見つかりませんでした。」|
| エラー | 「検索できませんでした。もう一度お試しください。」+ 再試行 |
| オフライン | 「オフライン中のため検索できません。」|

---

#### 通知画面 `(tabs)/notifications`

**概要:** いいね・コメント・フォロー・メンション等の通知一覧。

**画面要素:**
```
[ナビゲーションヘッダー]
  中央: 「通知」
  右: 「すべて既読にする」（アイコンボタン）

[通知リスト（FlatList / 無限スクロール）]
  各通知セル:
    [アバター（44pt）]
    [通知本文（例: 「○○さんがいいねしました」）]
    [対象投稿サムネイル（右端 / 画像投稿の場合）]
    [日時]
    [未読インジケータ（左端の色付きドット）]

[Pull-to-refresh]
```

**状態:**

| 状態 | 表示 |
|------|------|
| ローディング | セルスケルトン x 4 |
| 空 | 「まだ通知はありません。」|
| エラー | 「通知を読み込めませんでした。」+ 再読み込みボタン |
| オフライン | オフラインバナー |

**タップ挙動:** 通知をタップすると対象投稿詳細 or ユーザープロフィールへ遷移する（`lib/push/` の対応表で管理）。

---

#### プロフィール画面 `(tabs)/profile`（自分）

**概要:** 自分の投稿一覧・コメント一覧・フォロー関係を表示するプロフィール。

**画面要素:**
```
[ナビゲーションヘッダー]
  中央: ニックネーム
  右: 設定アイコン（→ settings へ遷移）

[プロフィールヘッダー]
  [カバー画像（任意）]
  [アバター（64pt / 左下に重なり）]
  [ニックネーム + @ユーザー識別子]
  [自己紹介文]
  [フォロワー数 / フォロー数（タップで一覧モーダルへ）]
  [プロフィール編集ボタン]

[タブ: 投稿 / コメント]
[タブ内 FlatList（各タブで無限スクロール）]
```

**状態:**

| 状態 | 表示 |
|------|------|
| ローディング | プロフィールヘッダースケルトン + PostCard スケルトン x 2 |
| 投稿なし（空） | 「まだ投稿がありません。」|
| エラー | 「プロフィールを読み込めませんでした。」+ 再読み込みボタン |
| オフライン | オフラインバナー |

---

#### もっと見る画面 `(tabs)/more`

**概要:** アプリ内のすべての機能とコンテンツへのアクセス入口を 1 画面にまとめたフルスクリーンのスクロール可能なメニュー画面。**ヘルプを除く全項目はネイティブ遷移**で提供し、ヘルプのみ expo-web-browser でアプリ内ブラウザを開く（**2026-07-13 更新:** 実装 `app/(tabs)/more/index.tsx` に合わせて全面改訂。旧版は大半の項目をアプリ内ブラウザで開く前提だった）。

詳細仕様は `docs/design/more-menu.md` を参照。

**画面要素（概要）:**
```
[タブヘッダー: 「もっと見る」]

[グループ 1（セクション見出しなし）]
  プロフィール（ネイティブ遷移 ›）
  メッセージ（ネイティブ遷移 ›）
  設定（ネイティブ遷移 ›）

[グループ 2: 機能]
  発見・盆栽用語辞典・施肥ガイド・植物ホルモン・農薬・病害虫・
  投稿分析・マイ盆栽・ブックマーク・盆栽園マップ・イベント・
  予約投稿（すべてネイティブ遷移 ›。全 11 項目）

[グループ 3: その他]
  利用規約・プライバシーポリシー・特商法表記（ネイティブ遷移 ›）
  ヘルプ（アプリ内ブラウザ ↗）

[グループ 4（セクション見出しなし）]
  ログアウト（colorError）
```

**状態:**

| 状態 | 表示 |
|------|------|
| ローディング | なし（静的メニューのためローディングなし）|
| 空 | 該当しない（静的メニューのため）|
| エラー | 該当しない（静的メニューのため）|
| オフライン | オフラインバナーを表示するが、メニュー自体は操作可能（ネイティブ遷移は動作する）|

---

### 4.3 タブ外スタック画面

#### 投稿詳細画面 `posts/[id]`

**概要:** 投稿本文・画像・アクションバー + コメントスレッドを表示。

**ナビゲーション:** タブスタックの上に Modal として積む（iOS: スライドアップシート or スタック遷移。Android: スタック遷移）。

**画面要素:**
```
[ナビゲーションヘッダー]
  左: 戻るボタン（iOS: "<" / Android: "←"）
  中央: 「投稿」

[投稿本文エリア（PostCard の disableNavigation=true 版）]

[コメント入力バー（画面下部 / キーボード上に追従）]
  [アバター（小）]
  [テキスト入力: 「コメントを追加...」]
  [送信ボタン]

[コメントリスト（FlatList / 無限スクロール）]
```

**iOS / Android 差異:**
- iOS: ヘッダーの戻るボタンは左端 "<" テキスト（デフォルト）、または「閉じる」（モーダル表示時）
- Android: ハードウェアバックボタンで戻る動作に対応する（Expo Router が処理）

**状態:**

| 状態 | 表示 |
|------|------|
| ローディング | 投稿スケルトン + コメントスケルトン x 2 |
| 投稿が存在しない | 「この投稿は見つかりません。」+ フィードへ戻るリンク |
| コメントなし（空） | 「まだコメントはありません。最初のコメントをしてみましょう。」|
| エラー | 「投稿を読み込めませんでした。」+ 再読み込みボタン |
| オフライン | オフラインバナー |

---

#### 他ユーザープロフィール画面 `users/[id]`

**概要:** 他ユーザーのプロフィール。フォロー・ブロック・通報アクションあり。

**自分のプロフィールとの差異:**
- 「プロフィール編集」の代わりに「フォロー / フォロー中」ボタン
- メニュー（⋮）から「ブロック」「通報」を提供（store-compliance.md 要件）

**状態:** 自分のプロフィールと同じ 4 状態 + 「非公開アカウント / フォローしていない」状態（投稿リストを非表示にする）

---

#### 新規投稿画面 `posts/new`

**概要:** テキスト・画像・ジャンルを入力して投稿を作成する。

**ナビゲーション:** フィード画面の FAB タップ → Modal として表示（iOS: ボトムシートまたはフルスクリーン。Android: フルスクリーン Modal）。

**画面要素:**
```
[ヘッダー]
  左: 「キャンセル」
  中央: 「新規投稿」
  右: 「投稿」ボタン（送信中は disabled + spinner）

[テキストエリア（自動拡縮）]
  プレースホルダー: 「盆栽の記録をシェアしましょう...」
  文字数カウンター（右下: 現在文字数 / 上限）

[画像エリア（横スクロール / 最大 4枚）]
  [+ 画像を追加ボタン]
  [各画像のサムネイル + 削除ボタン]

[ジャンル選択（最大 3 つ）]
  [タグリスト: 松柏類 / 雑木類 / 草もの / 用品・道具 / 施設・イベント / その他]

[文字数・画像数の上限表示]
  プレミアム: 2000文字 / 6枚
  無料: 500文字 / 4枚
```

**状態:**

| 状態 | 表示 |
|------|------|
| 送信中 | 投稿ボタンをスピナーに変更、全入力を disabled |
| 成功 | Modal を閉じてフィードに戻る。フィードを invalidate して最新表示 |
| エラー（400） | 入力エラーをインライン表示（文字数超過・禁止語等） |
| エラー（429） | 「本日の投稿上限に達しました。明日またお試しください。」|
| オフライン | 「投稿できません。ネットワーク接続を確認してください。」|

---

#### 投稿編集画面 `posts/[id]/edit`

新規投稿画面と同じレイアウト。見出しが「投稿を編集」になり、既存内容を初期値として表示。

---

#### 設定画面 `settings/`

**設定トップ画面 `settings/index`**

```
[グループリスト]
  プロフィール設定
  アカウント設定
  通知設定
  プレミアムプラン（購入 / 管理 / 復元）
  ブロックリスト
  ミュートリスト
  ヘルプ
  利用規約
  プライバシーポリシー
  ログアウト
```

**注意:** ログアウトと法的情報（利用規約・プライバシーポリシー・ヘルプ）は `(tabs)/more` からも到達できる（`more-menu.md` 参照）。設定画面のメニュー構成はそのまま維持する（重複導線は意図的）。

---

**アカウント設定画面 `settings/account`**

Google Play 審査要件（store-compliance.md）に基づき、**アカウント削除導線を必ず配置する。**

```
[メールアドレス変更]
[パスワード変更]
[---]
[危険ゾーン]
  [アカウントを削除する]（赤色テキスト）
```

アカウント削除タップ → 確認ダイアログ（2 ステップ確認）→ サーバー API 呼び出し → ログアウト処理（auth-tokens.md の手順に従う）。

---

**サブスクリプション画面 `settings/subscription`**

Google Play 審査要件に基づき、**購入の復元ボタンを必ず配置する。**
Stripe などの外部決済ページへの誘導は一切出さない（store-compliance.md）。

```
[現在のプラン表示]
  無料プラン or プレミアムプラン（有効期限）

[プレミアムの特典一覧]

[購入ボタン（未購入者向け）]
  ※ RevenueCat Offering から価格を取得して表示（価格ハードコード禁止）

[購入を復元]（既存購入者向け）

[Google Play 定期購入の管理はこちら]（Play の管理画面へ誘導）
```

**状態:**

| 状態 | 表示 |
|------|------|
| ローディング | プランスケルトン |
| 購入中 | スピナー + 「処理中です...」 |
| 保留（Pending） | 「決済が保留中です。Google Play でご確認ください。」|
| ユーザーキャンセル | エラー表示なし（静かにキャンセル） |
| ネットワークエラー | 「購入できませんでした。もう一度お試しください。」+ 再試行ボタン |

---

**ブロックリスト `settings/blocked`**

```
[ブロック中のユーザーリスト]
  各ユーザー: [アバター] [ニックネーム] [ブロック解除ボタン]

[空状態: 「ブロック中のユーザーはいません。」]
```

---

## 5. ヘッダー・戻る挙動の方針

### 5.1 ヘッダー種別

| 種別 | 適用場面 | 左要素 | 中央 | 右要素 |
|------|---------|-------|------|-------|
| タブヘッダー | (tabs) 配下 | ロゴ（オプション） | 画面タイトル | アクション（任意） |
| スタックヘッダー | posts/[id]、users/[id]等 | 戻るボタン | 画面タイトル | アクション（任意） |
| モーダルヘッダー | 投稿作成・編集 | 「キャンセル」テキストボタン | 画面タイトル | 「投稿」テキストボタン |

### 5.2 戻る挙動の OS 差異

| OS | 操作 | 挙動 |
|----|-----|------|
| iOS | ヘッダー左 "<" タップ | スタックをポップ（右から左へスライドバック） |
| iOS | 画面左端からのスワイプ | インタラクティブポップジェスチャー |
| Android | ヘッダー左 "←" タップ | スタックをポップ |
| Android | システムバックボタン / ジェスチャー | Expo Router が処理（スタックポップ or ホームへ） |
| 両 OS | モーダル「キャンセル」 | 未保存内容がある場合: 破棄確認ダイアログを出してから閉じる |

### 5.3 タブバーの表示・非表示

- (tabs) 配下の画面: BottomTabBar を常時表示
- スタック画面（posts/[id] 等）: BottomTabBar を非表示にする（タブの上に full-screen でスタック）
- モーダル画面（投稿作成等）: BottomTabBar を非表示にする

---

## 6. 4 状態の文言トーン方針

すべての画面にローディング / 空 / エラー / オフラインの 4 状態を定義する（error-handling.md 準拠）。

### 6.1 文言の基本トーン

- **落ち着いた丁寧語**（「〜できませんでした」「〜してください」）
- **原因の説明 + 解決策の提示**（「〜が原因です。〜でお試しください。」）
- **ユーザーを責めない**（「操作が正しくありません」などは使わない）
- **短く、明確に**（一文 40 字以内を目安）

### 6.2 状態別の文言パターン

| 状態 | パターン | 例 |
|------|---------|---|
| ローディング | スケルトンUIで表現（文言は極力出さない） | PostCard スケルトン |
| 空 | 「まだ〜がありません。〜してみましょう。」 | 「まだ投稿がありません。フォローしてみましょう。」|
| エラー | 「〜を読み込めませんでした。」+ 再試行ボタン | 「タイムラインを読み込めませんでした。」|
| オフライン | バナー: 「オフライン中です。接続が回復したら自動的に更新されます。」| 全画面共通バナー |
| 入力エラー | 「〜を入力してください。」/ 「〜は〜文字以内で入力してください。」| インラインエラー |
| 操作エラー(429) | 「〜の上限に達しました。〜またはお試しください。」| 「本日の投稿上限に達しました。明日またお試しください。」|
| 存在しない(404) | 「この〜は見つかりません。」+ 戻るリンク | 「この投稿は見つかりません。」|
| 権限なし(403) | 「この〜を表示する権限がありません。」| 非公開アカウントの投稿リスト |

---

## 7. ディープリンク方針

Push 通知タップ → 該当画面遷移のルーティングは `lib/push/` の 1 モジュールに集約する。
ディープリンクスキームは `bonlog://` とし、universal links（Android: App Links）は Phase 4 以降に検討。

| 通知種別 | 遷移先 |
|---------|-------|
| いいね | `posts/[postId]` |
| コメント | `posts/[postId]` |
| フォロー | `users/[followerId]` |
| メンション | `posts/[postId]` |
| リポスト | `posts/[postId]` |

未認証でディープリンク到達 → ログイン後に元の画面へ復帰（Expo Router の `redirect` + 遷移先の保持）。

---

## 8. ストア審査要件との対応（store-compliance.md 準拠）

| 要件 | 対応画面 | 備考 |
|-----|---------|-----|
| 通報 UI の露出 | `posts/[id]`（PostCard ヘッダー・コメント⋮）+ `users/[id]` ヘッダーメニュー + `events/[id]` ヘッダーメニュー + `shops/[id]` ヘッダーメニュー + `shops/[id]/reviews`（レビュー行の通報アイコン） | 投稿・コメント・ユーザー・イベント・盆栽園・レビューの 6 種すべてに対応（`ugc-safety.md` §7.1 の `REPORT_TARGET_TYPES` 準拠） |
| ブロック UI の露出 | `users/[id]` プロフィール画面 + `settings/blocked` | フォローボタン横のメニューからアクセス |
| アカウント削除 | `settings/account` | 2 ステップ確認 + Play Console データ削除申告 |
| 購入の復元 | `settings/subscription` | RevenueCat の restorePurchases 呼び出し |
| 外部決済誘導の禁止 | `settings/subscription` | Stripe ページへのリンクは一切出さない |

（2026-07-13 追記: `events/[id]`・`shops/[id]`・`shops/[id]/reviews` の通報導線実装を反映して上表の「通報 UI の露出」行を更新した。詳細仕様は `ugc-safety.md` §2.6・§7.1 を参照）

---

## 9. 未確定事項・引き継ぎ

- **フォロワーリスト / フォロー中リスト画面:** `users/[id]/followers`・`users/[id]/following`・`users/[id]/likes` としてネイティブ画面が実装済み（**2026-07-13 更新: §1 のツリーに反映**）。モーダル表示ではなく別画面遷移の形で確定している。
- **コメント詳細（ネスト）:** 現状コメントはフラット構造（Web と同様）として設計した。ネストが将来追加される場合は別途仕様を策定する。
- **投稿のリポスト / 引用投稿:** PostCardActions に含めているが、`posts/[id]/quote` として引用投稿画面は実装済み（**2026-07-13 更新: §1 のツリーに反映**）。リポスト選択 UI（BottomSheet）の詳細は別仕様として策定する必要がある。
- **画像フルスクリーンビューア:** 投稿詳細・プロフィールで画像タップ時のライトボックス表示は別途仕様策定が必要。`core に要相談`（presigned URL の有効期限とビューアのキャッシュ方針）。
- **オンボーディング画面 `(auth)/onboarding`:** 新規登録後のウェルカムフロー。§1 のツリーに該当ルートは存在しない（未実装）。MVP の優先度を PM に確認要。
- **「もっと見る」メニューの詳細仕様:** `docs/design/more-menu.md` に分離。項目追加・変更時はこのファイルと `more-menu.md` を両方更新すること（**2026-07-13: 両ファイルを実装に合わせて同時改訂済み**）。
- **非プレミアムユーザーへのプレミアム機能訴求:** `(tabs)/more` は購読状態による表示・非表示の分岐を行わず、予約投稿・投稿分析を全ユーザーへ常時表示する方式に確定した（**2026-07-13 更新:** 旧版の「グループ 4 を非プレミアムに完全非表示」という記載は実装と異なっていたため是正。プレミアム判定は遷移先の画面が担う。詳細は `more-menu.md` §3.2・§15 参照）。
- **§1 全体構造ツリーの再検証:** **2026-07-13 に完了。** `events/`・`shops/`・`dictionary/`・`pesticides/`・`fertilizers/`・`hormones/`・`bookmarks/`・`bonsai/`・`legal/`・`explore/`・`analytics/`・`messages/`・`follow-requests/`・`scheduled-posts/` 等、2026-06-20 以降に追加された全画面群を §1 のツリーへ反映した。
