# 通報・ブロック・ミュート UI/UX 仕様 — Bon_Log Native
<!-- このファイルは report-block-mute の仕様として機能します。ファイル名は ugc-safety.md です。 -->

作成日: 2026-06-15
更新日: 2026-07-13（通報対象をイベント・盆栽園・レビューへ拡張した実装〔`app/events/[id]/index.tsx`・`app/shops/[id]/index.tsx`・`app/shops/[id]/reviews/index.tsx`・`components/report/ReportDialog.tsx`・`lib/constants/report.ts`〕を反映。§1.2・§2.5・§2.6（新設）・§7.1・§7.2・§7.5・§8.1・§9・§10・§11・§12.1・§12.8（新設）・§15・§16 を更新。通報理由ラベル `inappropriate`/`harassment` を Web (`Bon_Log_cfw`) 整合値に是正）
前提: `design-tokens.md` / `navigation-structure.md` / `common-states.md` / `post-card.md` / `follow-and-engagement.md` に準拠
準拠規則: `.claude/rules/store-compliance.md`（通報・ブロックのアプリ内露出必須要件）

---

## 1. 概要・目的

Google Play ユーザー生成コンテンツポリシーの必須要件として、ユーザーが不快・有害なコンテンツやユーザーに対して**アプリ内から通報・ブロック・ミュートを実行できる導線**を提供する。

Web に機能が存在していてもアプリ内から実行できなければポリシー違反になるため、この仕様は機能要件と同格に扱う（`.claude/rules/store-compliance.md` §UGC 要件）。

### 1.1 解決する問題

- 不適切な内容・誹謗中傷を行うユーザーを安心して申告できる手段が不足していた
- ブロック・ミュートにより不快なユーザーの表示を制御できる手段がモバイルに存在しなかった
- ブロック中・ミュート中ユーザーの一覧と解除導線が設定画面にない状態だった

### 1.2 スコープ

| 機能 | MVP 対象 |
|------|---------|
| 投稿の通報 | 対象 |
| コメントの通報 | 対象 |
| ユーザーの通報 | 対象 |
| イベントの通報 | 対象（§2.6.1） |
| 盆栽園の通報 | 対象（§2.6.2） |
| レビューの通報 | 対象（§2.6.3） |
| ユーザーのブロック / 解除 | 対象 |
| ユーザーのミュート / 解除 | 対象 |
| ブロックリスト画面 | 対象 |
| ミュートリスト画面 | 対象 |

---

## 2. エントリポイントとメニュー設計

### 2.1 メニュー UI の基本方針（iOS / Android 共通）

「3点メニュー（⋮）」のタップでコンテキストメニューを表示する。**ボトムシート（ActionSheet 形式）** を使う。

```
iOS   : ActionSheetIOS（ネイティブ）
Android: Modal + リスト UI（カスタム）
```

Android カスタムシートの外形仕様:

```
背景    : colorSurface (#fcfcfc)
角丸    : radius2xl (18pt) ※上端のみ
影      : shadow-washi-lg
ハンドル: 幅 36pt / 高さ 4pt / colorBorderLight / 上端から spacing2
```

各メニュー項目の仕様:

```
高さ      : 56pt（タップターゲット 44pt 以上確保）
padding 水平: spacing5
アイコン  : 20pt / 左端
テキスト  : textMd (15pt) / colorTextPrimary
セパレータ: 1pt / colorBorderLight
```

破壊的アクション（通報・ブロック）は `colorError (#c0392b)` でテキスト・アイコンを表示する。

---

### 2.2 投稿カード「⋮」メニュー（PostCardHeader）

既存の `post-card.md` §13 を拡張・具体化する。

**表示条件による項目の切り替え:**

| 閲覧者 | 投稿者との関係 | 表示項目 |
|--------|--------------|---------|
| ログイン済み（自分の投稿） | `currentUserId === post.user.id` | 「編集」「プロフィールに固定」「削除」 |
| ログイン済み（他人の投稿） | `currentUserId !== post.user.id` | 「この投稿を非表示」「投稿を通報」「ユーザーをブロック」「ユーザーをミュート」 |
| 未認証 | — | ⋮ボタン自体を非表示にする |

**他人の投稿メニューのワイヤーフレーム:**

```
┌──────────────────────────────────────────┐
│ ▔▔▔▔▔▔▔▔▔▔（ハンドル）                  │
│ [EyeOff]  この投稿を非表示              │  colorTextPrimary
│ ─────────────────────────────────────── │
│ [Flag]    投稿を通報                    │  colorError
│ [Ban]     @{nickname} をブロック        │  colorError
│ [VolumeX] @{nickname} をミュート        │  colorError
│ ─────────────────────────────────────── │
│ [X]       キャンセル                    │  colorTextSecondary
└──────────────────────────────────────────┘
```

**ミュート状態・ブロック状態での項目変化:**

サーバーが `UserProfileResponse` に `isBlocked` / `isMuted` フラグを返す想定（正本は OpenAPI。**core に要確認**）。

- `isMuted === true` の場合: 「ミュート」を「ミュートを解除」に変更する
- ブロック中のユーザーの投稿はサーバー側でフィード除外済みのため、フィード内での「ブロック中」メニュー表示は原則発生しない。投稿詳細で `isBlocked === true` の場合は「ブロックを解除」を表示する

---

### 2.3 プロフィール画面「⋮」メニュー（`users/[id]`）

他ユーザープロフィールのヘッダー右上に `⋮` ボタン（44pt × 44pt）を配置する。自分のプロフィールには表示しない。

**フォローボタンとの位置関係:**

```
┌──────────────────────────────────────────────────────────┐
│  [アバター 64pt]                                          │
│  {ニックネーム}                                           │
│  @{ユーザー識別子}                                        │
│  {自己紹介文}                                             │
│  フォロワー {N}人  フォロー中 {N}人                       │
│                                                          │
│  [フォローボタン（flex: 1）]  [⋮ メニューボタン（44pt）] │
└──────────────────────────────────────────────────────────┘
```

`⋮` ボタン: 正方形 44pt / アウトラインボタン / `colorBorder` 枠線 1.5pt / `radiusMd`

**プロフィールメニューの項目（`isBlocked` / `isMuted` で動的に変化）:**

| `isBlocked` | `isMuted` | メニュー項目 |
|-------------|-----------|------------|
| false | false | 「ユーザーを通報」「ブロック」「ミュート」 |
| false | true | 「ユーザーを通報」「ブロック」「ミュートを解除」 |
| true | — | 「ユーザーを通報」「ブロックを解除」 |

ブロック中の場合はミュート項目を非表示にする（ブロックがミュートの上位概念のため）。

**未ブロック・未ミュート状態のワイヤーフレーム:**

```
┌──────────────────────────────────────────┐
│ ▔▔▔▔▔▔▔▔▔▔（ハンドル）                  │
│ [Flag]    @{nickname} を通報             │  colorError
│ [Ban]     @{nickname} をブロック         │  colorError
│ [VolumeX] @{nickname} をミュート         │  colorError
│ ─────────────────────────────────────── │
│ [X]       キャンセル                    │  colorTextSecondary
└──────────────────────────────────────────┘
```

---

### 2.4 コメントの通報導線

投稿詳細画面（`posts/[id]`）のコメントセルに「⋮」ボタンを配置する。

**コメントセルのレイアウト:**

```
┌──────────────────────────────────────────────────────────┐
│  [アバター 36pt]  {ニックネーム}  {日時}      [⋮ 44pt]  │
│                   {コメント本文}                          │
│                   [いいね]  [返信]                        │
└──────────────────────────────────────────────────────────┘
```

**コメントメニューの項目:**

| 条件 | 項目 |
|------|------|
| 自分のコメント | 「削除」（colorError） |
| 他人のコメント（ログイン済み） | 「コメントを通報」「コメント投稿者をブロック」「コメント投稿者をミュート」 |
| 未認証 | ⋮ボタン非表示 |

---

### 2.5 `store-compliance.md` との対応確認

| 要件 | 対応する導線 |
|------|------------|
| 通報 UI のアプリ内露出 | §2.2（投稿カード）/ §2.3（プロフィール）/ §2.4（コメント）/ §2.6（イベント・盆栽園・レビュー） |
| ブロック UI のアプリ内露出 | §2.2（投稿カード）/ §2.3（プロフィール）/ §3（ブロックフロー） |
| ブロック・ミュートのフィード・通知への即時反映 | §5（invalidation 方針） |
| ブロック解除導線 | §3.3（ブロック解除）/ §6.1（設定画面ブロックリスト） |

### 2.6 イベント・盆栽園・レビューの通報導線

投稿・コメント・ユーザー（§2.2〜§2.4）は `UserActionMenu`（共通ボトムシート）経由で通報・ブロック・ミュートをまとめて提供するのに対し、イベント・盆栽園・レビューはコンテンツそのものを対象とする通報専用の軽量な導線であり、`UserActionMenu` は使わない。**ブロック・ミュートはこれらの画面から提供しない**（イベント・盆栽園は投稿主ではなく掲載物自体が通報対象であり、レビューの通報もレビュー内容に対する通報に限定する。いずれもコンテンツ通報のみで、ユーザーへのブロック・ミュート導線はここには置かない）。

#### 2.6.1 イベント詳細画面のヘッダーメニュー（`events/[id]`）

ヘッダー右端の「⋮」ボタン 1 つを、閲覧者に応じて役割ごと出し分ける（通報専用の別ボタンを追加するのではなく、既存ボタンの動作を切り替える。実装: `app/events/[id]/index.tsx`）。

| 閲覧者 | 「⋮」タップ時の挙動 |
|--------|------|
| 主催者（`currentUser.id === event.creator.id`） | 所有者メニュー（「編集する」「削除する」。§3.4 相当。変更なし） |
| 非主催者・ログイン済み | 通報確認 Alert（下記） |
| 未ログイン | 「⋮」ボタン自体を表示しない |

通報確認 Alert（`Alert.alert` によるネイティブダイアログ。iOS は `UIAlertController`、Android は `AlertDialog` としてそれぞれの OS 標準の見た目のまま表示し、カスタムスタイルは当てない）:

```
タイトル: 「このイベントを通報しますか？」
ボタン: [通報する（destructive）]  [キャンセル]
```

「通報する」をタップすると `ReportDialog`（`targetType: 'event'`、`targetId`: イベント ID、`targetDisplayName`: イベント名）をモーダル表示し、以降は §7 の通報フローに合流する。

#### 2.6.2 盆栽園詳細画面のヘッダーメニュー（`shops/[id]`）

イベントと同じ「ボタン出し分け」方式（実装: `app/shops/[id]/index.tsx`）。

| 閲覧者 | 「⋮」タップ時の挙動 |
|--------|------|
| オーナー（`shop.isOwner === true`） | 所有者メニュー（「店舗情報を編集する」。削除項目はなし。変更なし） |
| 非オーナー・ログイン済み | 通報確認 Alert（下記） |
| 未ログイン | 「⋮」ボタン自体を表示しない |

```
タイトル: 「この盆栽園を通報しますか？」
ボタン: [通報する（destructive）]  [キャンセル]
```

「通報する」をタップすると `ReportDialog`（`targetType: 'shop'`、`targetId`: 店舗 ID、`targetDisplayName`: 店舗名）をモーダル表示する。

**共通の注記（2.6.1・2.6.2）:** ヘッダー「⋮」ボタンの `accessibilityLabel` は、所有者メニュー・通報メニューいずれの場合も固定文言（「イベントのメニューを開く」「店舗のメニューを開く」）のまま変えない。同一ボタンを閲覧者の役割に応じて出し分けているため、ラベル自体は操作対象（編集か通報か）を明示しない。

#### 2.6.3 盆栽園レビュー一覧の通報アイコン（`shops/[id]/reviews`）

レビュー一覧の各行に flag アイコンボタン（`flag-outline` / 44×44 タップ領域 / `colorTextSecondary`）を表示する（実装: `app/shops/[id]/reviews/index.tsx`）。イベント・盆栽園と異なり確認 Alert を挟まず、**タップで直接 `ReportDialog`（`targetType: 'review'`）を開く**（レビュー本文への通報は結果が明確で、誤タップの影響もイベント・盆栽園ページの編集/削除メニューに比べ小さいため）。

| 閲覧者 | flag アイコン表示 |
|--------|------|
| レビュー投稿者本人 | 非表示（自分のレビューは通報対象外） |
| 他人のレビュー・ログイン済み | 表示 |
| 未ログイン | 非表示 |

`accessibilityLabel`: 「この{`REPORT_TARGET_LABELS.review`}を通報する」→ 実際の表示は「このレビューを通報する」。`ReportDialog` の `targetDisplayName` は「{レビュー投稿者のニックネーム}のレビュー」。

**既知の追随候補（低）:** 盆栽園詳細画面（`shops/[id]`）冒頭のレビュープレビュー（最新3件・`ReviewPreviewItem`）には通報導線が未実装。通報したい場合は「すべてのレビューを見る」からレビュー一覧画面（`shops/[id]/reviews`）に遷移する必要がある。実装済み範囲・未実装範囲の追跡は §16 参照。

---

## 3. ブロックフロー

### 3.1 ブロック確認ダイアログ（必須）

ブロックは**相互フォローの自動解除**という不可逆な影響を伴うため、確認ダイアログを必ず表示する。

**ダイアログ仕様:**

```
┌──────────────────────────────────────────┐
│  @{nickname} をブロックしますか？         │  textLg / colorTextPrimary
│                                          │
│  ブロックすると、互いにフォローして      │  textBase / colorTextSecondary
│  いる場合はフォロー関係が解除され、      │
│  相手はあなたのプロフィールを検索・      │
│  フォローできなくなります。              │
│                                          │
│  [キャンセル]       [ブロックする]       │
└──────────────────────────────────────────┘
```

| ボタン | スタイル | 挙動 |
|--------|---------|------|
| 「キャンセル」 | テキストボタン / `colorTextSecondary` | ダイアログを閉じる |
| 「ブロックする」 | 塗りつぶしボタン / 背景 `colorError` / テキスト `colorTextInverse` / 高さ 44pt | §3.2 のブロック処理を実行 |

**iOS / Android 差異:**
- iOS: `Alert.alert()` のネイティブダイアログ（destructive アクションは OS が赤色で表示）
- Android: カスタムモーダルダイアログ（`colorError` 背景のボタンを使用）

**アクセシビリティ:**
- ダイアログ表示時にフォーカスをタイトルに移動させる
- `accessibilityRole="alert"` をダイアログコンテナに付与

---

### 3.2 ブロック送信フロー

```
確認ダイアログで「ブロックする」をタップ
  → ダイアログの「ブロックする」ボタンを disabled にする（スピナー表示）
  → POST /api/v1/users/{id}/block を呼び出す（想定エンドポイント。正本は OpenAPI）
  → 成功:
      ダイアログを閉じる
      成功トーストを表示:「@{nickname} をブロックしました」
      §5 の invalidation を実行
      遷移:
        投稿カードから操作した場合 → フィードへ戻り、投稿カードが消える（invalidate で再フェッチ）
        プロフィール画面から操作した場合 → §3.4 のブロック後表示に切り替える
  → 失敗:
      ダイアログのボタンを re-enable する
      エラートーストを表示（§3.5 参照）
      ダイアログは閉じない（ユーザーが再試行できる状態を維持）
```

---

### 3.3 ブロック解除フロー

ブロック解除は設定画面（`settings/blocked`）またはプロフィール画面のメニューから実行できる。

**確認ダイアログ:** 不要（ブロック解除はフォロー関係が復活しない限定的な影響のため）。

```
「ブロックを解除」をタップ
  → ボタンを disabled にする
  → DELETE /api/v1/users/{id}/block を呼び出す（想定エンドポイント。正本は OpenAPI）
  → 成功:
      成功トーストを表示:「@{nickname} のブロックを解除しました」
      §5 の invalidation を実行
      ブロックリスト（settings/blocked）から操作した場合 → リストアイテムを削除
      プロフィール画面から操作した場合 → メニューを「ブロック」に戻す
  → 失敗:
      ボタンを re-enable する
      エラートーストを表示（§3.5 参照）
```

---

### 3.4 ブロック後のプロフィール画面表示

`users/[id]` でブロック成功後にそのまま画面に留まる場合のレイアウト:

```
┌──────────────────────────────────────────────────────────┐
│  [アバター 64pt]                                          │
│  {ニックネーム}                                           │
│  @{ユーザー識別子}                                        │
│                                                          │
│  [ブロックを解除（colorActionSecondary）]  [⋮]           │
│  ─────────────────────────────────────────────────────── │
│  「このユーザーをブロックしています」                      │
│   colorTextSecondary / textBase / 中央寄せ               │
│                                                          │
│  （投稿リストは非表示）                                   │
└──────────────────────────────────────────────────────────┘
```

- 自己紹介・フォロワー数は省略する（ブロック中は閲覧できないこととする）
- 「ブロックを解除」ボタンはフォローボタンの位置に配置する

**ブロック後の画面遷移方針（未確定）:** ブロック直後に `users/[id]` に留まるか、前の画面に戻るかは PM の判断が必要（§16 参照）。

---

### 3.5 エラー処理（ブロック / ブロック解除）

| エラー | 使用定数 | 文言 |
|--------|---------|------|
| ネットワークエラー / ブロック | `ERR_BLOCK_FAILED`（既存 264行目） | 「ブロックに失敗しました。」 |
| ネットワークエラー / ブロック解除 | `ERR_UNBLOCK_FAILED`（既存 267行目） | 「ブロック解除に失敗しました。」 |
| 404（相手が存在しない） | `ERR_USER_NOT_FOUND`（既存 136行目） | 「このユーザーは見つかりません。」 |
| 429（レート制限） | `ERR_RATE_LIMIT`（既存 114行目） | 「操作が多すぎます。しばらく時間をおいてからお試しください。」 |
| 403（権限なし） | `ERR_FORBIDDEN`（既存 139行目） | 「この操作を行う権限がありません。」 |
| 409（既にブロック済み） | `ERR_BLOCK_ALREADY_BLOCKED`（新規 — §13） | 「すでにブロックしています。」 |
| オフライン | `ERR_OFFLINE_ACTION`（既存 22行目） | 「現在オフライン中のため、この操作はできません。接続を確認してください。」 |

---

## 4. ミュートフロー

### 4.1 確認ダイアログの要否

ミュートはフォロー関係を解除しない軽量な操作であり、誤操作しても容易に取り消せる。**確認ダイアログは表示しない**。タップ後、直ちに API を呼び出す。

---

### 4.2 ミュート送信フロー

```
「ミュート」をタップ（メニューから）
  → メニューを閉じる
  → POST /api/v1/users/{id}/mute を呼び出す（想定エンドポイント。正本は OpenAPI）
  → 成功:
      成功トーストを表示:「@{nickname} をミュートしました」
      §5 の invalidation を実行
  → 失敗:
      エラートーストを表示（§4.4 参照）
```

---

### 4.3 ミュート解除フロー

```
「ミュートを解除」をタップ（メニューまたは設定画面から）
  → DELETE /api/v1/users/{id}/mute を呼び出す（想定エンドポイント。正本は OpenAPI）
  → 成功:
      成功トーストを表示:「@{nickname} のミュートを解除しました」
      §5 の invalidation を実行
  → 失敗:
      エラートーストを表示（§4.4 参照）
```

---

### 4.4 エラー処理（ミュート / ミュート解除）

| エラー | 使用定数 | 文言 |
|--------|---------|------|
| ネットワークエラー / ミュート | `ERR_MUTE_FAILED`（新規 — §13） | 「ミュートに失敗しました。もう一度お試しください。」 |
| ネットワークエラー / ミュート解除 | `ERR_UNMUTE_FAILED`（新規 — §13） | 「ミュートの解除に失敗しました。もう一度お試しください。」 |
| 404 | `ERR_USER_NOT_FOUND`（既存 136行目） | 「このユーザーは見つかりません。」 |
| 429 | `ERR_RATE_LIMIT`（既存 114行目） | 「操作が多すぎます。しばらく時間をおいてからお試しください。」 |
| 409（既にミュート済み） | `ERR_MUTE_ALREADY_MUTED`（新規 — §13） | 「すでにミュートしています。」 |
| オフライン | `ERR_OFFLINE_ACTION`（既存 22行目） | — |

---

## 5. ブロック・ミュート成功後の即時反映（invalidation 方針）

サーバー側でブロック・ミュートユーザーはフィード・検索・通知から除外済みのため、クライアントの in-memory フィルタは不要。`invalidateQueries` でデータを再フェッチすることで除外済みデータが自動的に反映される。

### 5.1 ブロック成功後に invalidate するクエリキー

| クエリキー（想定） | 理由 |
|------------------|------|
| `posts.feed` | フィードからブロック相手の投稿を除去する |
| `users.detail(blockedUserId)` | プロフィールの `isBlocked` フラグを更新する |
| `users.followers` / `users.following` | フォロー関係の自動解除を反映する |
| `notifications` | ブロック相手の通知を除去する |
| `users.blocks` | 設定画面のブロックリストに追加する |
| `search.users` | 検索結果からブロック相手を除去する |

### 5.2 ミュート成功後に invalidate するクエリキー

| クエリキー（想定） | 理由 |
|------------------|------|
| `posts.feed` | フィードからミュート相手の投稿を除去する |
| `users.detail(mutedUserId)` | プロフィールの `isMuted` フラグを更新する |
| `notifications` | ミュート相手の通知を除去する |
| `users.mutes` | 設定画面のミュートリストに追加する |

クエリキーは `lib/queries/keys.ts` の命名に準拠する。invalidation 対応表 `lib/queries/invalidation-map.md` への追記を frontend に依頼する。

---

## 6. 設定画面（ブロックリスト / ミュートリスト）

### 6.1 ブロックリスト画面 `settings/blocked`

#### 画面構成

```
[スタックヘッダー]
┌──────────────────────────────────────────────────────────┐
│  [← 戻る]  ブロックリスト                               │
└──────────────────────────────────────────────────────────┘

[ブロック中のユーザーリスト（FlatList）]
┌──────────────────────────────────────────────────────────┐
│  [アバター 44pt]  {ニックネーム}        [ブロックを解除] │
│                   @{ユーザー識別子}                      │
├──────────────────────────────────────────────────────────┤
│  （繰り返し / セパレータ: 1pt colorBorderLight）          │
└──────────────────────────────────────────────────────────┘

[ページ末尾]
┌──────────────────────────────────────────────────────────┐
│  [スピナー] 読み込み中...  または「これ以上ありません」  │
└──────────────────────────────────────────────────────────┘
```

#### 「ブロックを解除」ボタン仕様

- スタイル: アウトラインボタン / 高さ 36pt / `hitSlop` で 44pt 確保 / `radiusMd` / `colorBorder` 枠
- テキスト: `textSm` / `colorTextPrimary`
- 確認ダイアログ: 不要（§3.3 の方針と同一）
- タップ後は §3.3 のブロック解除フローを実行する

#### 4 状態

| 状態 | 表示 |
|------|------|
| ローディング | `BlockedUserListItemSkeleton` × 4（アバター円 + 矩形 2 本 + ボタン矩形） |
| 空 | `ScreenEmpty` / icon: `UserX` 系 / 見出し:「ブロック中のユーザーはいません」/ 補足:「ブロックしたユーザーがここに表示されます」 |
| エラー | `ScreenError` / title:「読み込めませんでした」/ description: `ERR_BLOCK_LIST_LOAD_FAILED`（新規 — §13）/ onRetry: refetch |
| オフライン | `OfflineBanner` + キャッシュ表示（キャッシュなし: `ScreenError` で `ERR_OFFLINE_ACTION`） |

#### データの流れ

- `GET /api/v1/users/me/blocks`（カーソルベース / 想定エンドポイント。正本は OpenAPI）
- `useInfiniteQuery` + `nextCursor`（`data-fetching.md` 準拠）

---

### 6.2 ミュートリスト画面 `settings/muted`

#### 画面構成

ブロックリストと同一の構造。「ブロックを解除」が「ミュートを解除」に変わる。

```
[スタックヘッダー: ← ミュートリスト]

[ミュート中のユーザーリスト（FlatList）]
┌──────────────────────────────────────────────────────────┐
│  [アバター 44pt]  {ニックネーム}        [ミュートを解除] │
│                   @{ユーザー識別子}                      │
└──────────────────────────────────────────────────────────┘
```

#### 4 状態

| 状態 | 表示 |
|------|------|
| ローディング | `MutedUserListItemSkeleton` × 4 |
| 空 | `ScreenEmpty` / icon: `VolumeX` 系 / 見出し:「ミュート中のユーザーはいません」/ 補足:「ミュートしたユーザーがここに表示されます」 |
| エラー | `ScreenError` / title:「読み込めませんでした」/ description: `ERR_MUTE_LIST_LOAD_FAILED`（新規 — §13）/ onRetry: refetch |
| オフライン | `OfflineBanner` + キャッシュ表示（キャッシュなし: `ScreenError` で `ERR_OFFLINE_ACTION`） |

#### データの流れ

- `GET /api/v1/users/me/mutes`（カーソルベース / 想定エンドポイント。正本は OpenAPI）
- `useInfiniteQuery` + `nextCursor`

---

## 7. 通報フロー

### 7.1 通報対象種別

`lib/constants/report.ts` の `REPORT_TARGET_TYPES`（6 種）と 1 対 1 で対応する。

| 種別 | エントリポイント | 表示条件 | `targetType` 値 |
|------|--------------|---------|---------------|
| 投稿 | 投稿カード「⋮」→「投稿を通報」（`UserActionMenu`。§2.2） | ログイン済み・他人の投稿 | `post` |
| コメント | コメント「⋮」→「コメントを通報」（`UserActionMenu`。§2.4） | ログイン済み・他人のコメント | `comment` |
| イベント | イベント詳細ヘッダー「⋮」→ 確認 Alert →「通報する」（§2.6.1） | ログイン済み・非主催者 | `event` |
| 盆栽園 | 盆栽園詳細ヘッダー「⋮」→ 確認 Alert →「通報する」（§2.6.2） | ログイン済み・非オーナー | `shop` |
| レビュー | レビュー一覧の各行 flag アイコンを直接タップ（§2.6.3） | ログイン済み・自分以外のレビュー | `review` |
| ユーザー | プロフィール「⋮」→「ユーザーを通報」（`UserActionMenu`。§2.3） | ログイン済み・他人のプロフィール | `user` |

**Google Play UGC ポリシー適合:** 上記 6 種すべてにアプリ内の通報導線を実装済みであり、Google Play ユーザー生成コンテンツポリシーの「UGC 全般に対する通報手段をアプリ内で提供する」要件を満たす（`.claude/rules/store-compliance.md` §UGC 要件 準拠）。`navigation-structure.md` §8（ストア審査要件との対応）にも同旨を記載する。

---

### 7.2 通報フロー全体

```
エントリポイントで通報を開始する
  （投稿・コメント・ユーザー: メニューから「通報」を選択 / イベント・盆栽園: 確認 Alert の「通報する」/ レビュー: flag アイコンを直接タップ。§7.1 参照）
  → §7.3 の通報理由選択をボトムシートモーダルで表示
  → 理由を選択 → 「次へ」をタップ
  → §7.4 の詳細テキスト入力（同一モーダル内でステップを進める）
  → 「通報する」をタップ
  → §7.5 の送信処理
  → 成功 or エラー（§7.6）
```

---

### 7.3 通報理由選択（ステップ 1）

```
┌──────────────────────────────────────────────────────────┐
│  ▔▔▔▔▔▔▔▔（ハンドル）                                   │
│  ─────────────────────────────────────────────────────── │
│  通報の理由を選択してください         [× キャンセル]     │  textLg
│  ─────────────────────────────────────────────────────── │
│                                                          │
│  [○] スパム                                             │  高さ 56pt
│  [○] 不適切な内容                                       │
│  [○] 誹謗中傷                                           │
│  [○] 著作権侵害                                         │
│  [○] その他                                             │
│                                                          │
│  ─────────────────────────────────────────────────────── │
│  [次へ]（選択後に有効化）                                │
└──────────────────────────────────────────────────────────┘
```

各ラジオ行の仕様:
- 高さ 56pt（タップターゲット 44pt 以上）
- ラジオアイコン（選択: `colorActionPrimary` 塗り / 非選択: `colorBorder` 枠）
- テキスト: `textMd` / `colorTextPrimary`
- 未選択時の「次へ」: 不透明度 0.5 の disabled 状態
- 選択済み行: 背景 `colorSurfaceMuted` に変化

---

### 7.4 詳細テキスト入力（ステップ 2、任意）

```
┌──────────────────────────────────────────────────────────┐
│  ▔▔▔▔▔▔▔▔（ハンドル）                                   │
│  ─────────────────────────────────────────────────────── │
│  [← 戻る] 詳細を入力（任意）         [× キャンセル]     │
│  ─────────────────────────────────────────────────────── │
│                                                          │
│  選択した理由: {スパム / 不適切な内容 / ...}              │  textSm / colorTextSecondary
│                                                          │
│  ┌────────────────────────────────────────────────────┐  │
│  │ この通報についての詳細を入力してください（任意）   │  │
│  │                                                    │  │
│  │                                           0/1000   │  │
│  └────────────────────────────────────────────────────┘  │
│                                                          │
│  ─────────────────────────────────────────────────────── │
│  [通報する]                                              │
└──────────────────────────────────────────────────────────┘
```

テキストエリア仕様:
- 最大文字数: 1000 文字
- 残数カウンター: テキストエリア右下 / `textSm` / 900 文字以上で `colorError` に変化
- `KeyboardAvoidingView` で入力欄がキーボードに隠れないよう対応
- 背景: `colorSurfaceMuted` / 角丸 `radiusMd` / padding `spacing3`
- 詳細入力はスキップ可能（「通報する」ボタンは常時 enabled）

---

### 7.5 送信処理

```
「通報する」をタップ
  → ボタンを disabled にする（スピナー表示）
  → POST /api/v1/reports を呼び出す（実装: `lib/queries/moderation.ts` の `useReportMutation`。OpenAPI `CreateReportRequest` 準拠）
    リクエスト body:
      targetType: 'post' | 'comment' | 'event' | 'shop' | 'review' | 'user'
      targetId: string
      reason: 'spam' | 'inappropriate' | 'harassment' | 'copyright' | 'other'
      description?: string   ← 最大 1000 文字（`REPORT_DESCRIPTION_MAX_LENGTH`）
  → 成功:
      モーダルを閉じる
      成功トーストを表示:「通報を受け付けました。ご協力ありがとうございます。」
  → 失敗:
      ボタンを re-enable する
      §7.6 のインラインエラー表示
```

---

### 7.6 通報の成功・エラー状態

| 状態 | 表示方法 | 使用定数 / 文言 |
|------|---------|----------------|
| 成功 | トースト | 「通報を受け付けました。ご協力ありがとうございます。」 |
| 409（重複） | モーダル内インラインエラー | `ERR_REPORT_DUPLICATE`（新規 — §13）「この内容はすでに通報済みです。」 |
| 404（対象消失） | モーダル内インラインエラー | `ERR_REPORT_TARGET_NOT_FOUND`（新規 — §13）「通報対象が見つかりませんでした。」 |
| 429（レート制限） | モーダル内インラインエラー | `ERR_RATE_LIMIT`（既存） |
| 403（権限なし） | モーダル内インラインエラー | `ERR_FORBIDDEN`（既存） |
| ネットワークエラー | モーダル内インラインエラー | `ERR_REPORT_FAILED`（新規 — §13）「通報に失敗しました。もう一度お試しください。」 |
| オフライン | モーダル内インラインエラー | `ERR_OFFLINE_ACTION`（既存） |

重複（409）の方針: モーダルを閉じずインラインエラーを表示する。変更は PM が判断する。

---

### 7.7 通報フローの 4 状態

| 状態 | 表示 |
|------|------|
| ローディング（送信中） | 「通報する」ボタンをスピナーに変更・全入力 disabled |
| 空（理由未選択） | 「次へ」ボタンが disabled（不透明度 0.5） |
| エラー | §7.6 のインラインエラー。モーダルは閉じない |
| オフライン | `ERR_OFFLINE_ACTION` のインラインエラー。モーダルは閉じない |

---

## 8. コンポーネント分割

### 8.1 コンポーネント一覧と props 概要

**`UserActionMenu`（共通メニューシート）**

| prop | 型 | 説明 |
|------|-----|------|
| `targetUserId` | `string` | メニュー対象ユーザー ID |
| `targetUserNickname` | `string` | 表示用ニックネーム |
| `isOwnContent` | `boolean` | 自分のコンテンツかどうか |
| `contentType` | `ReportTargetType`（呼び出し元は現状 `'post' \| 'comment' \| 'user'` のみを使用。event/shop/review は本コンポーネントを使わず §2.6 の専用導線を持つ） | メニューの種別 |
| `contentId` | `string` | 対象コンテンツ ID |
| `isBlocked` | `boolean` | 対象ユーザーをブロック中か |
| `isMuted` | `boolean` | 対象ユーザーをミュート中か |
| `onClose` | `() => void` | メニューを閉じる |

**`ReportDialog`（通報モーダル）**

全 6 対象種別で共通利用する単一コンポーネント（`components/report/ReportDialog.tsx`）。投稿・コメント・ユーザーは `UserActionMenu` 経由、イベント・盆栽園・レビューは §2.6 の専用導線経由でそれぞれ開く。

| prop | 型 | 説明 |
|------|-----|------|
| `targetType` | `ReportTargetType`（`'post' \| 'comment' \| 'event' \| 'shop' \| 'review' \| 'user'`） | 通報対象種別 |
| `targetId` | `string` | 通報対象 ID |
| `targetDisplayName` | `string` | 表示用文字列 |
| `onClose` | `() => void` | モーダルを閉じる |

**`BlockConfirmDialog`（ブロック確認ダイアログ）**

| prop | 型 | 説明 |
|------|-----|------|
| `targetUserId` | `string` | ブロック対象ユーザー ID |
| `targetNickname` | `string` | 表示用ニックネーム |
| `onConfirm` | `() => void` | 確定ボタンタップ時 |
| `onCancel` | `() => void` | キャンセルボタンタップ時 |
| `isBlocking` | `boolean` | 送信中フラグ |

**`BlockedUserListItem`（設定画面ブロックリスト行）**

| prop | 型 | 説明 |
|------|-----|------|
| `user` | `{ id: string; nickname: string; userIdentifier: string; avatarUrl: string \| null }` | ユーザー情報 |
| `onUnblock` | `(userId: string) => void` | 解除ボタンタップ時 |
| `isUnblocking` | `boolean` | 解除処理中フラグ |

**`MutedUserListItem`（設定画面ミュートリスト行）**

| prop | 型 | 説明 |
|------|-----|------|
| `user` | `{ id: string; nickname: string; userIdentifier: string; avatarUrl: string \| null }` | ユーザー情報 |
| `onUnmute` | `(userId: string) => void` | 解除ボタンタップ時 |
| `isUnmuting` | `boolean` | 解除処理中フラグ |

**`ReportReasonSelector`（通報理由選択 / ReportDialog 内部）**

| prop | 型 | 説明 |
|------|-----|------|
| `selectedReason` | `'spam' \| 'inappropriate' \| 'harassment' \| 'copyright' \| 'other' \| null` | 選択中の理由 |
| `onChange` | `(reason: string) => void` | 選択変更時 |

**`ReportDetailInput`（詳細テキスト入力 / ReportDialog 内部）**

| prop | 型 | 説明 |
|------|-----|------|
| `value` | `string` | 入力テキスト |
| `onChange` | `(text: string) => void` | テキスト変更時 |
| `maxLength` | `number` | 最大文字数（1000） |

### 8.2 配置方針（案）

| コンポーネント | 配置パス |
|--------------|---------|
| `UserActionMenu` | `components/user/UserActionMenu.tsx` |
| `ReportDialog` | `components/report/ReportDialog.tsx` |
| `BlockConfirmDialog` | `components/user/BlockConfirmDialog.tsx` |
| `BlockedUserListItem` | `components/settings/BlockedUserListItem.tsx` |
| `MutedUserListItem` | `components/settings/MutedUserListItem.tsx` |
| `ReportReasonSelector` | `components/report/ReportReasonSelector.tsx` |
| `ReportDetailInput` | `components/report/ReportDetailInput.tsx` |

---

## 9. ナビゲーション

| 画面 / UI | 配置 | 遷移元 | 遷移先 | ディープリンク |
|-----------|------|--------|--------|-------------|
| `settings/blocked` | `settings/` スタック | `settings/index` のリスト行 | `users/[id]`（ユーザー行タップ） | 不要 |
| `settings/muted` | `settings/` スタック | `settings/index` のリスト行 | `users/[id]`（ユーザー行タップ） | 不要 |
| `ReportDialog` | Modal（各画面から表示） | 投稿カード・コメント・プロフィールのメニュー選択 / イベント・盆栽園の通報確認 Alert（§2.6.1・§2.6.2）/ レビュー一覧の flag アイコン（§2.6.3） | 閉じると元の画面に戻る | 不要 |
| `BlockConfirmDialog` | オーバーレイ（Alert / カスタム Modal） | ブロックメニュー選択時 | 閉じると元の画面に戻る | 不要 |

`navigation-structure.md` §4.3 の設定トップ画面に「ブロックリスト」「ミュートリスト」が既に定義済みのため、設定画面への項目追加は不要。

---

## 10. データの流れ（API 依存）

以下は**想定エンドポイント**。正本は OpenAPI スペック（Bon_Log_cfw 管轄）。

| 操作 | メソッド | 想定パス |
|------|---------|---------|
| 通報 | `POST` | `/api/v1/reports` |
| ブロック | `POST` | `/api/v1/users/{id}/block` |
| ブロック解除 | `DELETE` | `/api/v1/users/{id}/block` |
| ミュート | `POST` | `/api/v1/users/{id}/mute` |
| ミュート解除 | `DELETE` | `/api/v1/users/{id}/mute` |
| ブロックリスト取得 | `GET` | `/api/v1/users/me/blocks` |
| ミュートリスト取得 | `GET` | `/api/v1/users/me/mutes` |

**通報エンドポイントは実装済み:** `POST /api/v1/reports` は `lib/queries/moderation.ts` の `useReportMutation`（`components/report/ReportDialog.tsx` から呼び出し）で実装済み。`targetType` は `post` / `comment` / `event` / `shop` / `review` / `user` の 6 種に対応する（`lib/constants/report.ts` の `REPORT_TARGET_TYPES`）。

**`UserProfileResponse` への追加フィールド（cfw に要確認 — 最優先）:**

- `isBlocked: boolean` — 閲覧者が対象ユーザーをブロックしているか
- `isMuted: boolean` — 閲覧者が対象ユーザーをミュートしているか

これらのフラグがない場合 N+1 呼び出しが発生するため、`UserProfileResponse` への埋め込みをサーバー側に依頼すること（**core に要相談**）。

**`PostResponse` 内 `user` オブジェクトへの `isBlocked` / `isMuted` 埋め込みも core に要相談。**

---

## 11. アクセシビリティ

| 要素 | 仕様 |
|------|------|
| メニューシート全体 | `accessibilityViewIsModal={true}` で背景コンテンツへのフォーカスを防ぐ |
| 各メニュー項目 | `accessibilityRole="button"` / `accessibilityLabel`: 文言と同文 |
| 破壊的項目（通報・ブロック） | `accessibilityHint`: 「この操作は取り消せない場合があります」 |
| ブロック確認ダイアログ | 表示時にフォーカスをタイトルに移動 / `accessibilityRole="alert"` / `accessibilityLiveRegion="assertive"` |
| 「通報する」ボタン（disabled 時） | `accessibilityState: { disabled: true }` |
| 通報モーダルのキャンセル | `accessibilityLabel`: 「通報をキャンセルして閉じる」 |
| ブロック解除ボタン（リスト） | `accessibilityLabel`: 「@{nickname} のブロックを解除する」 |
| ミュート解除ボタン（リスト） | `accessibilityLabel`: 「@{nickname} のミュートを解除する」 |
| イベント/盆栽園ヘッダー「⋮」（通報 Alert 経由時） | Alert 自体は OS ネイティブが a11y を扱う。ボタンの `accessibilityLabel` は「イベントのメニューを開く」「店舗のメニューを開く」で固定（§2.6.1・§2.6.2） |
| レビュー通報アイコン（flag） | `accessibilityRole="button"` / `accessibilityLabel`: 「このレビューを通報する」/ 44×44 タップ領域（§2.6.3） |
| 全タップ要素 | 44pt 以上（`hitSlop` で補完） |
| ラジオ行（理由選択） | `accessibilityRole="radio"` / `accessibilityState: { checked: boolean }` |

---

## 12. コピー案（文言一覧）

### 12.1 通報理由ラベル

`lib/constants/report.ts` の `REPORT_REASON_LABELS`（Web `Bon_Log_cfw` の表示文言と一致させる）。

| enum 値 | 表示ラベル |
|---------|-----------|
| `spam` | スパム |
| `inappropriate` | 不適切な内容 |
| `harassment` | 誹謗中傷 |
| `copyright` | 著作権侵害 |
| `other` | その他 |

### 12.2 メニュー項目

| 場面 | 文言 |
|------|------|
| 投稿カード / 通報 | 「投稿を通報」 |
| 投稿カード / ブロック | 「@{nickname} をブロック」 |
| 投稿カード / ミュート | 「@{nickname} をミュート」 |
| 投稿カード / ミュート解除 | 「@{nickname} のミュートを解除」 |
| 投稿カード / ブロック解除（詳細画面） | 「@{nickname} のブロックを解除」 |
| プロフィール / 通報 | 「@{nickname} を通報」 |
| プロフィール / ブロック | 「@{nickname} をブロック」 |
| プロフィール / ミュート | 「@{nickname} をミュート」 |
| プロフィール / ミュート解除 | 「@{nickname} のミュートを解除」 |
| プロフィール / ブロック解除 | 「@{nickname} のブロックを解除」 |
| コメント / 通報 | 「コメントを通報」 |
| コメント / ブロック | 「@{nickname} をブロック」 |
| コメント / ミュート | 「@{nickname} をミュート」 |
| キャンセル（全メニュー共通） | 「キャンセル」 |

### 12.3 ブロック確認ダイアログ

| 要素 | 文言 |
|------|------|
| タイトル | 「@{nickname} をブロックしますか？」 |
| 本文 | 「ブロックすると、互いにフォローしている場合はフォロー関係が解除され、相手はあなたのプロフィールを検索・フォローできなくなります。」 |
| キャンセルボタン | 「キャンセル」 |
| 確定ボタン | 「ブロックする」 |

### 12.4 通報モーダル

| 要素 | 文言 |
|------|------|
| ステップ 1 見出し | 「通報の理由を選択してください」 |
| ステップ 2 見出し | 「詳細を入力（任意）」 |
| 詳細入力プレースホルダー | 「この通報についての詳細を入力してください（任意）」 |
| 送信ボタン | 「通報する」 |
| キャンセルボタン | 「キャンセル」 |
| 「次へ」ボタン | 「次へ」 |
| 成功トースト | 「通報を受け付けました。ご協力ありがとうございます。」 |

### 12.5 ブロック・ミュート操作の成功トースト

| 操作 | 文言 |
|------|------|
| ブロック成功 | 「@{nickname} をブロックしました」 |
| ブロック解除成功 | 「@{nickname} のブロックを解除しました」 |
| ミュート成功 | 「@{nickname} をミュートしました」 |
| ミュート解除成功 | 「@{nickname} のミュートを解除しました」 |

### 12.6 プロフィール画面（ブロック中）

| 要素 | 文言 |
|------|------|
| ブロック中の説明文 | 「このユーザーをブロックしています」 |
| ブロック解除ボタン | 「ブロックを解除」 |

### 12.7 設定画面

| 場面 | 文言 |
|------|------|
| ブロックリスト画面タイトル | 「ブロックリスト」 |
| ミュートリスト画面タイトル | 「ミュートリスト」 |
| ブロック解除ボタン（リスト行） | 「ブロックを解除」 |
| ミュート解除ボタン（リスト行） | 「ミュートを解除」 |
| ブロックリスト空状態 見出し | 「ブロック中のユーザーはいません」 |
| ブロックリスト空状態 補足 | 「ブロックしたユーザーがここに表示されます」 |
| ミュートリスト空状態 見出し | 「ミュート中のユーザーはいません」 |
| ミュートリスト空状態 補足 | 「ミュートしたユーザーがここに表示されます」 |
| リスト終端 | 「これ以上ありません」 |

### 12.8 通報対象種別ラベル・イベント/盆栽園/レビュー通報コピー

`lib/constants/report.ts` の `REPORT_TARGET_LABELS`（Web と一致）。Alert タイトルや `accessibilityLabel` の「この{対象}を通報しますか？」「この{対象}を通報する」に埋め込む。

| enum 値 | 表示ラベル |
|---------|-----------|
| `post` | 投稿 |
| `comment` | コメント |
| `event` | イベント |
| `shop` | 盆栽園 |
| `review` | レビュー |
| `user` | ユーザー |

| 場面 | 文言 |
|------|------|
| イベント通報確認 Alert タイトル | 「このイベントを通報しますか？」 |
| イベント通報確認 Alert ボタン | 「通報する」/「キャンセル」 |
| 盆栽園通報確認 Alert タイトル | 「この盆栽園を通報しますか？」 |
| 盆栽園通報確認 Alert ボタン | 「通報する」/「キャンセル」 |
| レビュー通報アイコン accessibilityLabel | 「このレビューを通報する」 |

---

## 13. `lib/constants/errors.ts` に必要な新規定数

以下の定数を `lib/constants/errors.ts` に追加すること（frontend への依頼事項）。

| 定数名 | 文言 | 使用場面 |
|--------|------|---------|
| `ERR_MUTE_FAILED` | `'ミュートに失敗しました。もう一度お試しください。'` | ミュート失敗トースト |
| `ERR_UNMUTE_FAILED` | `'ミュートの解除に失敗しました。もう一度お試しください。'` | ミュート解除失敗トースト |
| `ERR_REPORT_FAILED` | `'通報に失敗しました。もう一度お試しください。'` | 通報送信失敗インラインエラー |
| `ERR_REPORT_DUPLICATE` | `'この内容はすでに通報済みです。'` | 409 重複通報インラインエラー |
| `ERR_REPORT_TARGET_NOT_FOUND` | `'通報対象が見つかりませんでした。'` | 404 通報対象消失インラインエラー |
| `ERR_BLOCK_ALREADY_BLOCKED` | `'すでにブロックしています。'` | 409 重複ブロックエラー |
| `ERR_MUTE_ALREADY_MUTED` | `'すでにミュートしています。'` | 409 重複ミュートエラー |
| `ERR_BLOCK_LIST_LOAD_FAILED` | `'ブロックリストを読み込めませんでした。'` | ブロックリスト取得失敗 |
| `ERR_MUTE_LIST_LOAD_FAILED` | `'ミュートリストを読み込めませんでした。'` | ミュートリスト取得失敗 |

流用する既存定数（変更不要）:

| 定数名 | errors.ts 行 |
|--------|-------------|
| `ERR_BLOCK_FAILED` | 264 行目 |
| `ERR_UNBLOCK_FAILED` | 267 行目 |
| `ERR_FORBIDDEN` | 139 行目 |
| `ERR_RATE_LIMIT` | 114 行目 |
| `ERR_OFFLINE_ACTION` | 22 行目 |
| `ERR_USER_NOT_FOUND` | 136 行目 |

---

## 14. `follow-and-engagement.md` §2.6 文言統一の決定記録

前回評価指摘の対応として、`follow-and-engagement.md` §2.6 の 403 エラー文言「この操作は実行できません。」を `ERR_FORBIDDEN`（「この操作を行う権限がありません。」）に統一した。

**決定理由:**

1. CLAUDE.md 核心ルール6「エラーメッセージは `lib/constants/errors.ts` の定数を使用」に従い、インライン文字列を使わない
2. フォロー操作専用の 403 文言を新設するほどの意味的差異がなく、`ERR_FORBIDDEN` で十分に意図が伝わる
3. 全画面で同じ 403 エラーに同じ文言を使う方が、ユーザーの一貫した体験につながる

**変更済みファイル:**
- `docs/design/follow-and-engagement.md` §2.6 の表の 403 行を更新
- §12 に決定内容のメモを追記
- ヘッダーに「更新日: 2026-06-15」を追記

---

## 15. 既存との一貫性メモ

| 既存要素 | 本仕様での扱い |
|---------|-------------|
| `post-card.md` §13（3点メニュー） | 「投稿を通報」「ブロック」「ミュート」の項目定義と `UserActionMenu` 委譲を本仕様で具体化した |
| `navigation-structure.md` §4.3（settings/blocked / settings/muted） | 既存定義の画面に内部 UI（リスト・解除ボタン・4 状態）を本仕様で追加した |
| `navigation-structure.md` §8（ストア審査対応表） | 「通報 UI の露出」「ブロック UI の露出」の確認先が本仕様に対応する。イベント・盆栽園・レビューの追加分は本仕様 §2.6・§7.1 に対応する（§8 の表も本改訂で更新） |
| `follow-and-engagement.md` §2.4（楽観更新） | ブロック・ミュートは楽観更新しない（影響範囲が広く、フォロー解除という副作用があるため、送信完了後に invalidate する） |
| `events.md` §3.4（ヘッダー「⋮」メニュー） | 非作成者・ログイン済みユーザー向けの通報導線（確認 Alert → `ReportDialog`）は本仕様 §2.6.1 に委ねる。作成者向けの編集・削除メニューは `events.md` 側の記述が正 |
| `shops.md` §3.6・§5.2（ヘッダー「⋮」メニュー・ReviewItem） | 非オーナー向け通報導線は本仕様 §2.6.2、レビュー一覧の flag アイコンは §2.6.3 に委ねる。オーナー向け編集メニューは `shops.md` 側の記述が正 |
| `common-states.md` 4 状態コンポーネント | 全リスト画面・通報モーダルで `ScreenLoading` / `ScreenEmpty` / `ScreenError` / `OfflineBanner` を使用 |
| `design-tokens.md` `colorError`（`#c0392b`） | 破壊的アクション（通報・ブロック）のメニュー項目・確認ダイアログの確定ボタンに適用 |
| `design-tokens.md` `shadow-washi-lg` | ボトムシートメニューの影に適用 |

---

## 16. 未確定事項・要判断

| 項目 | 内容 | 判断者 |
|------|------|--------|
| `UserProfileResponse` への `isBlocked` / `isMuted` フィールド追加 | サーバー側（cfw）への API 変更要求。これがないとプロフィール画面とメニューの状態を適切に表示できない | core（最優先） |
| `PostResponse` 内 `user` オブジェクトへの `isBlocked` / `isMuted` 埋め込み | 投稿カードのメニューでの状態表示に必要 | core |
| ブロックリスト / ミュートリスト API のカーソルページネーション対応 | CLAUDE.md 核心ルール5（offset 不使用）の観点から、未対応の場合はサーバー側の対応が必要 | core |
| 通報の `detail` フィールドのサーバー受け取り | `POST /api/v1/reports` が `detail` フィールドを受け取る設計になっているか | core |
| ブロック後のプロフィール画面の挙動 | ブロック直後に `users/[id]` に留まるか（§3.4）、前の画面に戻るか | PM |
| 通報重複（409）のモーダル挙動 | §7.6 で「モーダルを閉じずインラインエラーを表示」としたが、閉じる方針への変更は PM が判断する | PM |
| フォローリクエスト通知からの通報導線 | フォローリクエストを送ってきたユーザーの通報は通知一覧から非対応。将来のフォローリクエスト管理画面追加時に検討する | PM |
| 盆栽園詳細のレビュープレビュー（`ReviewPreviewItem`。最新3件）に通報導線がない | 現状は `shops/[id]/reviews` の一覧画面のみ通報可能。プレビューへの flag アイコン追加は既知の追随候補（低優先度）。§2.6.3 参照 | PM（低優先度） |
